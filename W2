@echo off
setlocal enabledelayedexpansion
set count=0
set max=10

:loop
echo Running script at %date% %time%
REM YourCommandHere

set /a count+=1
if !count! geq %max% goto end

timeout /t 60 /nobreak >nul
goto loop

:end
echo Finished running %max% times.
