echo === Mapped Drives === >> "%LOGFILE%"
for /f "skip=2 tokens=1,2,*" %%A in ('net use') do (
    if not "%%A"=="" if not "%%C"=="" (
        echo Drive %%A -> %%C >> "%LOGFILE%"
    )
)
echo. >> "%LOGFILE%"
